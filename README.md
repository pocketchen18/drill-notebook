# Drill Notebook（面向开发者）

| 项 | 说明 |
|---|---|
| 平台 | Windows 优先，**绿色便携**（数据默认不写系统目录） |
| 架构 | Electron 壳 + React/TS 渲染进程 + 内嵌 Spring Boot（Java）+ SQLite |
| 通信 | 后端仅监听 `127.0.0.1`，前端 HTTP 调用本地 API |
| 版本线索 | `git log`：`v0.3` PDF/导出/高级挑题；`v0.4` 记忆曲线 + 日历 + AI 多会话；`v0.5` AI 总结知识点 + 背知识点重构 + 笔记本混合检索（BM25 + 可选向量 RAG）；`v0.5.1` 会话内短周期记忆曲线（循环背诵全流程重构）+ 题库选题继承 + 会话↔日历联动（顽固项加练、评分最终态、日历实时今天）；`v0.5.2` 界面状态记忆（启动回到上次页面 + 各页选择/切换/筛选持久化）；`v0.5.3` 常规设置完善（主题跟随系统 + AI 悬浮球开关 + 自定义快捷键：全局 + 页面内）；`v0.6`（进行中）前端 UI 全面重塑；`v0.7`（规划）AI 跨会话记忆 |

---

## 目录

1. [功能列表（按版本）](#1-功能列表按版本)
2. [操作与交互逻辑](#2-操作与交互逻辑)
3. [技术架构与目录](#3-技术架构与目录)
4. [本地开发与验证](#4-本地开发与验证)
5. [文档索引](#5-文档索引)
6. [约束与安全](#6-约束与安全)

---

## 1. 功能列表（按版本）

### 1.1 基础功能（v0.1–v0.2 骨架 + 持续增强）

| 模块 | 功能摘要 |
|---|---|
| **题库** | 题库 CRUD；题型：`single` / `multiple` / `fill` / `true_false` / `essay`；Markdown 导入（`===` 分题 + YAML frontmatter）；重复导入按内容哈希跳过 |
| **刷题** | 会话抽题、提交判分、进度与快捷键；可「加入计划」「问 AI」「添加到笔记」 |
| **错题** | 最近一次答错且未纠正的题目列表；再练 / 加入计划 / AI 分析 |
| **笔记本** | TipTap 编辑器；KaTeX / Mermaid / Markdown 块默认渲染、点击编辑；自动保存；可加入计划 |
| **AI 助手** | 全局悬浮球 + 侧栏（`Ctrl+J`）；页面上下文注入；回复插入笔记；密钥加密；请求经 Java 代理 |
| **设置** | 主题（浅色 / 深色 / 跟随系统）、**自定义快捷键（全局 + 页面内）**、AI 悬浮球开关、界面状态记忆、**主模型**与**导入兜底模型**分轨配置、复习方案、会话偏好 |
| **便携运行** | `APP_ROOT` 下数据 / 日志 / 缓存 / PID；不写注册表依赖 |

### 1.2 v0.3

| 功能 | 说明 |
|---|---|
| **PDF 导入** | 规则解析为主；可配置 AI 兜底解析（见 `docs/import-formats.md`） |
| **统一导入 AI 兜底** | PDF / Markdown / JSON 等路径可走同一套 AI 补全逻辑 |
| **导出** | 题库 / 笔记 / 错题等支持 **Markdown、PDF、HTML** 等导出；可自定义勾选、多选（见前端 `ExportActions`） |
| **高级挑题与打乱** | 刷题 / 背题 / 背知识点：按题型、章节、标签等筛选；随机 / 手动顺序 / 组内保序等策略（`AdvancedQuestionSelector`） |
| **知识点导入** | Markdown 分级导入，可 AI 兜底与自定义分类（`docs/knowledge-point-import.md`） |

### 1.3 v0.4

| 功能 | 说明 |
|---|---|
| **记忆曲线（SM-2 风格）** | 题目 / 知识点加入复习方案；EF、间隔、重复次数、状态流转；答错策略可配；多方案（标准 / 考前突击 / 保守等） |
| **背题 / 背知识点全流程** | 与复习方案深度集成：方案选择、仅待复习、评分（选项交互 / 0–5 等）；会话结束可推荐加入计划 |
| **学习日历** | 年/月导航；按日查看计划组；完成勾选、去学习；同日可重复安排同一资源 |
| **加入计划** | 各学习面一键添加：起始日必填、终止日可选；可选 AI 排程（失败规则降级） |
| **会话结束推荐** | 刷题 / 背题 / 背知识点结束后弹窗「本轮结束后的学习计划」 |
| **今日队列** | 日历侧：记忆曲线到期项 + 当日计划待办合并；可续进度 |
| **日历 SRS 叠加** | 日期格上显示到期/逾期标记（与手动计划独立） |
| **AI 多会话** | 新建 / 切换 / 重命名 / 删除会话；消息 AES-GCM 加密持久化到 SQLite；可导出会话 |

### 1.4 v0.5

| 功能 | 说明 |
|---|---|
| **背知识点重构** | 树状大纲阅读与卡片工作区：左侧多级大纲导航（支持搜索过滤、按标签筛选、悬停完整标题气泡 Tooltip、统一层级缩进步长、视口自适应独立双向滚动） + 右侧卡片工作区；点卡片或按全屏按钮进入 `KnowledgeFullCardView` 全屏阅读视图（支持大纲拖拽调宽与阈值自动折叠、键盘快捷翻页、章节/整库/单卡总结与还原、修改/删除按钮，`Escape` 退出） |
| **AI 总结知识点** | 丰富维度：①总结并导入（选 `.md`/`.txt` → AI 浓缩成标准导入格式 → 入库）②整库/章节总结（对未总结卡逐条存原文+AI 浓缩+更新 `content`）③重新总结（对已总结卡从原文重跑）④未单独配置导入兜底模型时自动无缝回退主模型。详见 [docs/ai-summary.md](docs/ai-summary.md) |
| **原文/总结双态切换** | 知识卡片标签栏提供单卡「显示原文 / 显示总结」切换；全屏视图支持单卡/章节/整库一键切换；后端提供 `restore-original` / `restore-summary` 批量恢复接口，`knowledge_point_original` 表存 `role=original`/`summary` 双快照，`knowledge_point.content` 在两态间切换 |
| **笔记本混合检索（RAG）** | AI 侧栏对话可注入笔记本上下文。**BM25 关键词检索默认启用、无需下载**（FTS5 trigram，笔记保存即索引）；向量检索为 opt-in 增强。两层经 RRF 融合成统一引用列表，点 citation 深链跳回页面。详见 [docs/ai-retrieval.md](docs/ai-retrieval.md) |
| **检索范围与降级** | 范围 `all` / `current`（当前笔记本）；向量层任何故障静默降级为 BM25-only 并附 `retrievalNotice`，**对话不受影响** |
| **本地 embedding 模型目录** | 设置页「嵌入 / 模型目录」：内置 `bge-small-zh-v1.5`（512 维，MIT），**默认不下载**；下载校验 size/sha256、断点续传、可取消、可彻底卸载（模型文件 + 向量） |
| **远程 embedding（OpenAI/Ollama）** | 可选；**必须先勾选「同意发送笔记内容」**，授权绑定 endpoint/model，更换后失效。远程不可达时降级 BM25，对话仍 200 |
| **离线索引维护** | 启动回填旧页（不阻塞启动）、全库重建（missing/full）、失败重试、模型切换后旧向量异步清理 |
| **离线 embedding worker** | Rust 子进程经 NDJSON 协议提供本地向量化；schema v8（chunk/FTS/model/space/embedding/job 六表） |
| **PDF 导入版式参照样本** | `backend/src/test/resources/fixtures/pdf/sample-for-import-test.pdf` 作为版式 Y 的活模板，4 题全走规则路径成功 |
| **导出格式 round-trip** | 题库导出的叙述式 `.md`（`### 题干` + `**答案：**` + `---` 分隔）可直接用「导入 Markdown」入口再导回，新增 `ExportMarkdownParser` 解析；无需手动改成 frontmatter 格式 |
| **健康端点加固** | 生产构建移除健康接口敏感路径暴露；AI 日志脱敏；移除 `console` 残留 |
| **冒烟与种子脚本** | `scripts/mvp-test.ps1`（后端核心接口冒烟）、`scripts/seed-test-bank.py`（Python 造题种子） |
| **笔记本附件** | 图片/PDF/DOCX/视频/压缩包等文件拖拽或选择上传；内联预览（图片 inline、PDF 嵌入、DOCX 预览），其余提供下载提示；后端 SHA-256 去重，存储于便携数据目录 `data/attachments/` |
| **视频嵌入** | 支持 YouTube / Bilibili 链接嵌入 + 本地/远程视频文件直接播放；三种视图一键切换：**链接视图**（蓝色高亮可编辑链接，单击浏览器打开、双击编辑）、**标题视图**（显示标题，同链接交互）、**预览视图**（iframe/原生 `<video>` 直接播放，骨架屏加载态与错误兜底）；菜单支持复制链接 / 在浏览器打开 |
| **笔记本全屏** | 顶栏「全屏」按钮一键切换，`Escape` 退出；全屏模式隐藏侧栏与页面导航，专注编辑与阅读 |

### 1.5 v0.5.1（会话内短周期记忆曲线 · 循环背诵）

| 功能 | 说明 |
|---|---|
| **练习两级结构** | 侧栏「练习」= 刷题 ｜ 背诵；背诵内含「背题（题库）/ 背知识点」子切换，背诵全流程在练习页内完成 |
| **多轮循环出场** | 所选条目每轮按编排顺序全部出场，共循环 R 轮（默认 3，1–10 可配）；会话界面按轮分组（跳转面板轮次分组、进度「第 r / R 轮」标签） |
| **错题重复（百词斩式）** | 默认**组末复习**：每轮按组（默认 10 题）分段，答错插入本组末尾重现；可切「本轮末尾」「延迟重现」；额外重复上限可配（0=直到会为止） |
| **过关与次轮顺序** | 连对达标算过关，可选「过关后跳过后续轮次」；下一轮可选保持原序 / 错题优先 / 随机 |
| **背诵设置弹窗 + 预设** | 「记忆曲线 · 摘要」按钮打开弹窗：4 预设（快速过一遍 / 推荐三轮 / 百词斩式 / 强化五轮）+ 全部自定义项 + 出场规模预估；与设置页「会话内记忆曲线」卡片共用一份 localStorage 配置（详见 [docs/session-curve.md](docs/session-curve.md)） |
| **背知识点专用选材页** | 轻量知识树级联勾选（勾父带子、节点带计数），**只背叶子卡片**；不再复用知识点阅读页，阅读页回归纯阅读 |
| **题库选题继承** | 题库页勾选后「开始练习」跳转 `/quiz?bankId=…&questionIds=…&from=bank`，刷题页预选并可继续调整；无勾选按整库；错题「错题再练」不受影响 |
| **会话 → 日历/跨天曲线联动** | 跨天评分以**会话最终判定**为准（重复同判定不重复推远，判定翻转才补交 `forceAdvance`）；会话内答错 ≥2 次或已放弃的条目识别为**顽固项**，会话结束弹窗内默认勾选，自动排入**明天 + 后天**各一轮加练并写入日历（后端零改动） |
| **日历实时「今天」** | `useToday()` 每 60 秒 + 窗口聚焦校准真实日期；跨零点自动刷新计划/到期/今日队列缓存，停留在今天时选中日自动跟随，翻看其他日期不打断 |
| **兼容边界** | 刷题固定单轮 + 延迟重现（旧行为）；日历 / 今日队列 / 深链会话强制单轮；旧版 localStorage 配置自动钳位迁移 |

### 1.6 v0.5.2（界面状态记忆）

| 功能 | 说明 |
|---|---|
| **启动回到上次页面** | 只记 pathname（不记查询参数），无记忆时默认打开「笔记本」；`/quiz`、`/memorize` 等深链别名归到「练习」，不会在下次启动自动续考（详见 [README §2.13](#213-界面状态记忆v052)） |
| **各页选择与切换记忆** | 笔记本（当前本/页/勾选/专注模式）、题库（当前库 + 每库各自的题目勾选）、知识点（库/搜索/标签/选中卡片/树折叠）、练习（Tab、背题｜背知识点子模式、各库勾选、两个挑题器的筛选）、错题勾选、日历视图年月、设置分区 |
| **单一存储 + 钳位归一** | `frontend/src/lib/viewState.ts` 一个 localStorage key（`ui.viewState.v1`），`normalizeViewState` 容错解析（损坏 JSON、未知页、非法 id 一律丢弃）；写入按页浅合并 + JSON 去重 + 300ms 防抖，并在 `pagehide/beforeunload` 兜底冲刷 |
| **体积与语义护栏** | 整库勾选折算成「全选」哨兵，零散勾选上限 500 个 id、树勾选 200 个 key、每类只留最近 4 个题库作用域，整体超预算先截断再放弃写入；**绝不**持久化密码、脏缓冲与进行中的会话进度 |
| **失效自愈** | 记忆中的题库/笔记本/页/卡片被删除后自动回落该页默认第一项；挑题器恢复时剪掉已不存在的章节/标签筛选 |
| **开关与清除** | 设置 → 通用「记住上次停留位置与各页选择」（默认开），关闭即清空；另有「清除已记住的位置」按钮 |

### 1.7 v0.5.3（常规设置完善）

| 功能 | 说明 |
|---|---|
| **主题跟随系统** | 设置 → 常规「主题」三选一：浅色 / 深色 / 跟随系统；跟随系统时监听 `prefers-color-scheme`，系统切换深浅色即时生效；顶栏开关或快捷键切换会自动转为显式模式 |
| **AI 悬浮球开关** | 可隐藏右下角悬浮球；快捷键与设置页「打开 AI 助手」按钮仍可唤出侧栏 |
| **自定义快捷键（全局 + 页面内）** | 设置 → 常规 → 快捷键按作用域分组：**全局**（打开 / 关闭 AI 助手 默认 `Ctrl+J`、切换深浅主题、打开设置）、**刷题**（提交答案 `Enter`/`Ctrl+S`、下一题 `Enter`/`→`/`PgDn`/`N`、上一题 `←`/`PgUp`/`P`）、**知识卡片全屏**（搜索 `Ctrl+F`、折叠 / 展开大纲 `T`、上一张 `←`、下一张 `→`、退出 `Esc`）、**笔记编辑器**（完成块编辑 `Ctrl+Enter`）、**题库**（重命名 `F2`）、**笔记本**（重命名笔记页 `F2`）、**AI 助手**（发送 `Enter`）。每个动作可绑多个组合键：点「+」录入，Esc 取消，Backspace 清空该项，chip 上的 × 移除单个，单项 / 全部恢复默认。规则：全局与编辑器内动作须含 Ctrl 或 Alt（F1–F12 可单独），页面内动作允许单键（输入框聚焦时不生效）；AI 发送只能是 Enter 组合；全局动作与任何动作互斥，同一页面内除不同阶段（刷题作答中 / 已提交）外互斥，不同页面可共用；录制期间全局监听让路。所有提示文案（顶栏、悬浮球 title、刷题页底部、卡片工具提示、编辑器提示、AI 输入框占位）跟随当前绑定。实现见 `frontend/src/lib/shortcuts.ts` + `hooks/useGlobalShortcuts.ts`，各页面用 `matchesAny` / `resolveShortcutAction` 读 `uiStore.shortcutConfig`；存储 `localStorage['ui.shortcuts.v1']`（每项 string[]，兼容早期单串） |

### 1.8 v0.6（进行中）前端 UI 全面重塑

| 功能 | 说明 |
|---|---|
| **视觉系统重做** | `frontend/src/styles/app.css` 统一设计令牌（色板 / 字阶 / 4px 间距 / 圆角 4–16 / 三级阴影 / 120–240ms 动效），明暗两态同源；在 `body` 上覆盖 Arco 主题变量（`--arcoblue-*`、圆角、文本 / 边框 / 填充色）让 Arco 组件随令牌换肤，不再逐组件补丁 |
| **品牌与图标** | 新品牌标识（渐变方块 + 折角页 + 对勾）：母版 `resources/icon/icon.svg`；`npm run build:icons` 用 Electron 自带 Chromium 渲染出 `icon.png`（512）与多尺寸 `icon.ico`（窗口图标 + 便携 exe 图标，打包随 `extraResources` 拷入）；侧栏品牌行（`BrandMark`）与 `frontend/public/favicon.svg` 复用同一几何 |
| **壳层重排** | 侧栏改为分组导航（资料 / 学习 / 规划 + 底部设置），折叠 / 3 秒自动隐藏 / 悬停提示逻辑不变；顶栏改为页面标题 + 「AI 助手」快捷键药丸 + 明暗切换图标按钮 |
| **页面精修** | 错题、知识点、练习（刷题 / 背诵：题卡圆角、选项字母徽章、进度条、反馈卡）、日历（今天徽章、月份头部自适应换行）、设置（pill Tab、分隔行）与 AI 侧栏（气泡、会话栏）逐页重构视觉层；笔记本与题库保持工作台几何契约（`DESIGN.md`）只做视觉微调 |
| **约束** | 不改变现有功能与交互逻辑，仅做不破坏性的微调（折叠按钮展开态显示「收起侧栏」文字、主题开关改为图标按钮）；既有测试与几何契约测试继续通过 |
| **随手修补** | 笔记本命令行补回「新建笔记本」入口（v0.5 工作台重构时遗失，后端接口一直存在）；练习选题器、知识点关联题目改为 Markdown 内联渲染，错题表格 / 计划标题 / 下拉选项等纯文本场景用 `lib/markdownText` 剥离 Markdown 与 LaTeX 记号；AI 连接提示与后端报错统一为「Base URL + API Key」措辞 |
| **笔记本 / 页面重命名** | 页面标题原先每敲一个字符就 PUT 一次，且从不失效 `['note-page', pageId]`，输入框被服务端旧标题顶回去，看起来「改不了名」；现改为本地成稿、失焦或回车提交一次，Esc 放弃。页面列表行支持双击标题或按 `F2`（作用域「笔记本」，设置 → 常规 → 快捷键可改绑）改名；笔记本在命令行的下拉旁新增改名按钮，编辑时下拉临时换成输入框。提交后直接回写 `['notebooks']` / `['note-pages', id]` / `['note-page', id]` 缓存，列表、编辑区标题、AI 引用同步更新 |
| **快捷键不再带动滚动条** | 知识卡片全屏内命中的快捷键（上一个 / 下一个 / 折叠大纲 / 退出）一律 `preventDefault`，方向键、PageUp/PageDown 等不再同时滚动大纲横向或正文纵向滚动条；输入框 / 文本域 / contentEditable 内的按键交还控件本身；大纲跟随高亮改为只滚动大纲自己的滚动容器 |
| **RAG 修复** | ① `embedding-worker` 可执行文件由 `electron/java-bridge.ts` 与 `scripts/start-mvp.ps1` 同序自动定位（release → debug 兜底）并注入后端，`npm run dev:electron` / 打包启动不再因缺少 `DRILL_EMBEDDING_WORKER_EXE` 而永久「索引构建中」；② poller 在 REBUILDING 且队列无可运行任务时重算 coverage 并按需激活，队列被 supersede 清空后不再卡住；③ `videoBlock` / `fileBlock` 的标题、链接、文件名纳入索引（此前只含视频块的页面 chunk 数为 0，检索与 AI 都读不到）；④ 启动时新增 normalizer 审计：已索引页的 `content_hash` 与当前 normalizer 不符即重建分块 / FTS / 向量任务，这是 normalizer 变更的迁移通道（否则旧页要等用户手动编辑才会修复）；⑤ `retrieval/status` 新增 `providerReady`，设置页据此区分「索引构建中」与「本地向量组件不可用」 |

### 1.9 v0.7（规划，尚未实现）

| 功能 | 说明 |
|---|---|
| **AI 跨会话记忆** | 侧栏对话跨会话持久记忆（独立 corpus，不改变笔记本数据所有权与向量空间不变量）。原列入 v0.6，因 UI 重塑优先而顺延 |

### 1.10 明确后置（未作为当前目标）

- 知识图谱、完整考试模拟、多人协作  
- SQLCipher 整库加密、自然语言改写日期控件等  

---

## 2. 操作与交互逻辑

以下为**推荐操作步骤**（UI 文案以中文界面为准）。

### 2.1 启动应用

1. 双击 `start-mvp.cmd`，或执行 `npm start`。  
2. 首次会安装依赖并构建前端 / Electron / 后端 jar。  
3. 开发数据默认写在便携数据根目录（由 `APP_ROOT` / 启动脚本决定，不入库）。  
4. 关闭 Electron 窗口应停止本地 Java 后端。

环境检查（不启动 GUI）：`npm run start:check`  
强制重建后启动：`npm start -- -Rebuild`

### 2.2 题库：导入与维护

1. 打开侧栏 **题库**。  
2. **新建题库** 或选中已有题库。  
3. **导入 Markdown**：选择符合格式的 `.md`（见 [2.9](#29-markdown-题库格式)）；重复题自动跳过。  
4. **导入 PDF**（v0.3）：选择 PDF；规则解析失败或不足时，若已配置 AI，可触发 AI 兜底。  
5. 在题目列表可编辑、删除、勾选后 **导出**（MD/PDF/HTML 等，以界面按钮为准）。  
6. 勾选部分题目后点 **开始练习** 会携带勾选跳转刷题页（可继续调整）；未勾选按整库进入；或将题目 **加入计划** / 加入复习方案（设置中的复习方案）。

### 2.3 刷题

1. **刷题** → 选择题库与题量（或从错题「再练」、从计划「去学习」带参进入）。  
2. 可使用 **高级挑题**（题型/章节/标签等）与打乱选项。  
3. 答题快捷键：`1–4` 选选项，`Enter` 提交；`←`/`→`、`PageUp`/`PageDown` 或 `P`/`N` 切题（除数字外均可在设置 → 常规 → 快捷键改绑，底部提示跟随当前绑定）。  
4. 提交后查看对错与解析；可将题 **添加到笔记**、**问 AI**、**加入计划**。  
5. **会话结束**后：弹出「本轮结束后的学习计划」→ 勾选候选 → 设起始/终止日 → 可选「让 AI 帮忙排计划」→ 写入日历。

### 2.4 背诵（背题 / 背知识点）

1. **练习 → 背诵** → 子切换选择「背题（题库）」或「背知识点」。  
2. 选择范围与顺序：背题用高级挑题（筛选/全选取消、随机重排、拖拽编排）；背知识点在知识树上勾选范围（见 2.4.3）。  
3. 点「开始」旁 **「记忆曲线 · 摘要」** 按钮可调整会话内短周期曲线（见 2.4.2 与 [docs/session-curve.md](docs/session-curve.md)）；评分同时写入跨天复习方案（SM-2）。  
4. 会话中按界面评分（背题 / 背知识点均为「会 / 不会」，映射 SM-2 quality 4 / 0）；答错条目按记忆曲线策略在会话内重复出现。  
5. 可随时 **加入计划**；结束后走会话结束推荐弹窗（可将需复习条目加入今日队列 / 未来三天 / 自定义区间），弹窗底部的 **顽固项加练** 默认勾选，会把本会话反复答错的条目排进明天、后天的日历（见 2.4.4）。

#### 2.4.1 知识点阅读页（v0.5 树状大纲与工作区 UI，仅供阅读）

1. **知识点**（侧栏）→ 顶部选择题库 → 左侧大纲树导航，右侧为卡片工作区。阅读页保持纯阅读；主动背诵请去「练习 → 背诵 → 背知识点」。  
2. **多级大纲浏览**：
   - 点击章节目录查看递归章节内容，点击最顶层总根目录查看全库总览。支持按关键词搜索和按标签快速过滤。
   - **标题悬停完整提示**：鼠标悬浮在大纲各级标题上时自动以精致的气泡（Tooltip）显示完整标题文本。
   - **统一层级缩进与独立滚动**：各级目录严格按照统一阶梯递进缩进；大纲面板自适应视口高度，底部完整可见，支持独立纵向滚动滑至最底端以及深层级的横向左右滑动。  
3. **卡片阅读与切换**：
   - 标签栏右侧提供单卡「显示原文 / 显示总结」快捷切换。
   - 点击右上角「全屏」按钮进入 `KnowledgeFullCardView` 全屏视图。
4. **全屏视图交互**：  
   - **大纲拖拽伸缩**：支持鼠标拖拽左侧大纲边界调节宽度（180px~600px），向左拖拽小于 120px 阈值时自动平滑折叠（类似 VSCode 大纲体验）；默认快捷键 `T` 折叠/展开（可在设置 → 常规 → 快捷键改绑，同组还有搜索 `Ctrl+F`、翻页 `←`/`→`、退出 `Esc`）。  
   - **快捷翻页**：支持键盘 `←` / `→` 快速切换上一张/下一张知识点。  
   - **总结 / 还原**：支持单卡、章节分支及根节点（整库）一键总结与还原；无总结时点此触发 AI 总结。  
   - **重新总结**：对已总结卡片/章节从原文重跑 AI 浓缩。  
   - **修改** / **删除**：关全屏进编辑器 / Popconfirm 确认删除当前卡片。  
   - `Escape` 退出全屏。  
5. **AI 总结与兜底机制**：  
   - 顶部 **AI 总结** 按钮支持「总结并导入」、「总结当前知识库」与「重新总结当前知识库」。  
   - 当未单独配置「导入兜底」模型时，自动平滑回退使用「主模型」配置。  
   细节见 [docs/ai-summary.md](docs/ai-summary.md)。

#### 2.4.2 会话内循环背诵（v0.5.1 短周期记忆曲线）

1. 弹窗（「记忆曲线 · 摘要」按钮打开）中设置：**循环轮数**（默认 3）、**错题重复策略**（组末复习 / 本轮末尾 / 延迟重现）、**每组题数**、**额外重复上限**（0=直到会为止）、**过关连对次数**、**过关跳过后续轮**、**下一轮顺序**（原序/错题优先/随机）；顶部 4 个预设一键套用。  
2. 会话界面：进度条显示「第 r / R 轮」与重现「第 k 遍」标签；跳转面板按轮分组，重现条目橙色虚线；同一知识点每轮可独立评分。  
3. 答错条目按策略插队重复；达到额外重复上限显示「本次未记住」，后续轮次仍会出场。  
4. 全部轮次完成后弹会话结束推荐；日历 / 今日队列深链进入的会话**强制单轮**（定时任务只出场一次）；刷题模式不受循环配置影响（保持单轮延迟重现）。  
5. 评分写入跨天曲线时**以会话最终判定为准**：重复同一判定不再重复推进，只有「会 ↔ 不会」翻转时才补交一次（`forceAdvance`）；会话结束弹窗中的**顽固项加练**（见 2.4.4）决定要不要把顽固条目排进明天、后天。

#### 2.4.3 背知识点选材（只背叶子卡片）

1. 左侧轻量知识树：级联勾选（勾父带子），节点标题带子树计数；支持全选 / 清空。  
2. **父节点、章节节点只用于圈定范围，不进入背诵清单**；计数与开始按钮均按叶子卡片统计。  
3. 背诵顺序 = 文档树顺序；「开始背知识点（N）」进入 2.4.2 会话流程。

#### 2.4.4 会话 ↔ 日历联动（顽固项加练）

1. 会话中**额外答错 ≥ 2 次**或**重复用尽仍未过关**的题目 / 知识卡片判为「顽固项」（对应 Anki leech、百词斩顽固错词）。  
2. 会话结束弹出「本轮结束后的学习计划」时，底部出现 **顽固项加练（N）** 卡片，**默认勾选**，列出条目名，可随时取消。  
3. 保持勾选并提交 → 除常规候选的计划组外，额外写入 **「顽固项加练 · 第 1 天 / 第 2 天」两个计划组（明天、后天各一轮）**，固定排在紧邻两天，不受你选的日期窗口均分影响；顽固项即使没出现在常规候选里，也会一并「加入记忆曲线」。  
4. 关闭「加入学习日历」时卡片内会提示排程不会写入；只开「加入记忆曲线」则仅入队、不写日历。  
5. 到点后这些条目出现在**日历对应日期**与**今日队列**中，按既有计划项流程「去学习」即可。细节见 [docs/session-curve.md](docs/session-curve.md) 第 5 节。

### 2.5 错题

1. 打开 **错题**。  
2. 列表为「最近一次答错且尚未纠正」的题目。  
3. **再练一遍** 进入刷题会话；可 **AI 分析错题**、**加入计划**。

### 2.6 笔记本

1. **笔记本** → 选择/新建笔记本与页面。  
2. 编辑：正文 + 工具栏插入 **公式 / 图表 / Markdown 块**。  
3. **默认渲染**；**点击块**进入编辑；完成/失焦或 `Ctrl/⌘+Enter` 结束编辑。  
4. 自动保存状态显示在标题旁。  
5. **附件**：拖入或通过文件选择器上传图片、PDF、DOCX、视频、压缩包等文件；图片/PDF/DOCX 在内联预览区直接显示，其余附件提供下载提示。  
6. **视频嵌入**（v0.5）：  
   - 支持 YouTube / Bilibili 链接、远程视频 URL、本地视频文件；  
   - **三种视图**一键切换（块顶部 ▾ 菜单）：**链接视图**（蓝色高亮可编辑链接，单击浏览器打开，双击编辑）、**标题视图**（显示标题，同链接交互）、**预览视图**（iframe 嵌入 / 原生 `<video>` 控件播放，含骨架屏加载态与播放失败兜底）；  
   - 菜单支持复制链接、在浏览器打开。  
7. **全屏展示**（v0.5）：顶栏「全屏」按钮一键切换，`Escape` 退出；全屏模式隐藏侧栏与页面导航，专注编辑与阅读。  
8. 当前页或勾选多页可 **加入计划**；AI 回复可「插入笔记」。

### 2.7 日历与今日队列

1. **日历**：切换年/月；点击日期查看当日计划组。  
2. 计划组：完成勾选、删除、**去学习**（跳转刷题/背知识点/笔记等）。  
3. 日期格可能显示 **计划待办数** 与 **SRS 到期/逾期** 标记。  
4. 页面上的 **今日队列**：合并「今日记忆曲线到期」与「当日计划」；答完可自动下一项；刷新后尽量续进度。  
5. **实时「今天」**：日历每 60 秒（以及窗口重新聚焦时）对齐系统日期；跨过零点会自动刷新计划、到期统计与今日队列，若你正停留在「今天」则选中日跟随进入新的一天，正在翻看其它日期时不会被跳走。

### 2.8 加入计划（通用弹窗逻辑）

1. 在刷题 / 背题 / 知识点 / 错题 / 笔记等处点 **加入计划**。  
2. **起始日必填**，**终止日可选**。  
3. **不开启 AI**：  
   - 无终止日 → 全部写到起始日；  
   - 有终止日 → 在区间内按天轮询均分条目。  
4. **开启 AI 排计划**：填写需求提示词 → 在日期窗口内生成多日方案（无终止时默认约 5 天窗口）→ 确认写入；失败则规则降级。  
5. 到 **日历** 检查并「去学习」。

### 2.9 Markdown 题库格式

- 题与题之间单独一行 `===`。  
- 每题 YAML frontmatter：`type`、`answer`（按题型）、`tags`、`chapter` 等。  
- 选择题：选项 + `answer`；判断：`true`/`false`；填空：文本答案；解答题可无固定答案，可写参考答案。  
- 细则与 PDF/JSON：`docs/import-formats.md`、`docs/quiz-markdown-flow.md`。

### 2.10 AI 助手

1. 右下角悬浮球或 **`Ctrl+J`**（默认，可在设置 → 常规 → 快捷键改绑）打开侧栏；悬浮球可在设置里隐藏。  
2. **设置 → AI 连接**：分别配置 **主模型**（对话/总结/计划）与 **导入兜底模型**（PDF/MD/JSON/知识点解析），密钥加密存储。  
3. 在刷题/错题/笔记等页打开时，可自动带上 **当前页面上下文**（可关闭「使用」）。  
4. 支持多会话：新建 / 切换 / 双击标题重命名 / 删除；消息落库加密。  
5. 助手消息可 **插入笔记**（选笔记本+页面）。  
6. 可导出会话（Markdown / HTML / JSON，以菜单为准）。  
7. **笔记本检索（v0.5）**：侧栏内开启「检索」，选范围 `全部笔记本` / `当前笔记本`；回答下方列出引用来源，点击跳回对应页面。向量检索需在设置页启用本地模型或远程 embedding（见 [docs/ai-retrieval.md](docs/ai-retrieval.md)）。

### 2.11 设置

1. **界面**：主题三选一（浅色 / 深色 / 跟随系统，同步 Arco `arco-theme` + `html[data-theme]`）；AI 悬浮球显示开关。  
2. **便携**：应用根目录、数据库路径、后端健康状态。  
3. **AI 连接**：见上。  
4. **复习方案**：创建/编辑/删除方案；间隔表、EF、答错策略、每日新学/复习上限、优先级；默认方案标记。  
5. **会话偏好**：是否默认加入复习、默认计划、强制推进等（localStorage）。  
6. **界面状态记忆**：「记住上次停留位置与各页选择」开关（默认开启）+「清除已记住的位置」按钮；关闭时立即清空已存状态。  
7. **快捷键**（v0.5.3）：常规分区按「全局 / 刷题 / 知识卡片全屏 / 笔记编辑器 / 题库 / AI 助手」分组，每个动作可绑多个组合键；点「+」录入新键，Esc 取消，Backspace 清空该项，× 移除单个，行内图标还原单项，「全部恢复默认」一键还原；会在同一时机触发的重复组合键会被拒绝。  

### 2.12 主题（深色模式）

1. 顶栏开关切换 `light` / `dark`。  
2. 实现要点（开发者）：  
   - `document.documentElement.dataset.theme`  
   - `document.body` 的 `arco-theme="dark"`（Arco 组件与 Portal）  
   - CSS 变量定义在 `html` / `:root`，避免 Drawer 挂到 body 时丢变量  
   - 本地 `localStorage['drill-notebook-theme']` 作首屏兜底；Electron `config` 持久化  
3. **跟随系统**（v0.5.3）：`localStorage['ui.themeMode']` 存 `light` / `dark` / `system`；`system` 时首屏（`main.tsx`）与运行时（`App.tsx` 监听 `prefers-color-scheme` 变化）都按系统深浅色解析，Electron `config.theme` 不再覆盖；顶栏开关或快捷键切换会转为显式模式。旧版仅有 `drill-notebook-theme` 时自动迁移为对应显式模式。  

### 2.13 界面状态记忆（v0.5.2）

1. **启动落点**：关闭软件后再打开，回到上次停留的页面；**没有记忆时默认打开「笔记本」**。未知/失效的地址同样落到这里。  
2. **各页记住的内容**：  
   - 笔记本：当前笔记本、当前页、勾选的页面、专注模式；  
   - 题库：当前题库，以及**每个题库各自的**题目勾选（切回某库不再被清空）；  
   - 知识点：当前知识库、标题搜索、标签筛选、当前选中的卡片、大纲树折叠；  
   - 练习：刷题 / 背诵 Tab 与「背题（题库）｜背知识点」子模式、各库题目勾选、背知识点用的知识库与勾选范围、两个挑题器的筛选条件；  
   - 错题：勾选的题目；日历：翻到的年/月；设置：所在分区。  
3. **不记忆的东西**：做到一半的练习会话（背题/刷题/今日队列的队列位置与已答状态）、弹窗与输入草稿、任何密码（如题库主密码）——重启后回到各页的选材界面，勾选仍在，点开始即重新排队。  
4. **深链优先**：从日历「去学习」、题库「开始练习」、错题「错题再练」等入口进入时，链接里带的参数覆盖记忆值；记忆只存路径不存查询串（`/quiz`、`/memorize` 归到「练习」），因此下次启动落在练习的选材界面，不会自动续考。  
5. **失效自愈**：记忆中的题库/笔记本/页面/卡片被删除后自动回落该页默认第一项；勾选列表过长时按「全选」哨兵或截断存储，不谎报选择。  
6. **开关与清除**：设置 → 通用 → 「记住上次停留位置与各页选择」；关闭即清空，也可随时点「清除已记住的位置」手动重置。  

---

## 3. 技术架构与目录

```
┌─────────────────────────────────────────────────────────┐
│  Electron 主进程（窗口、便携路径、单实例、拉起/回收 Java）   │
└───────────────────────────┬─────────────────────────────┘
                            │ spawn + HTTP 127.0.0.1
┌───────────────────────────▼─────────────────────────────┐
│  Spring Boot + SQLite（题库/会话/计划/SRS/AI 配置与会话）  │
└───────────────────────────┬─────────────────────────────┘
                            │ fetch API
┌───────────────────────────▼─────────────────────────────┐
│  React + Arco + TipTap + Zustand + React Query            │
└─────────────────────────────────────────────────────────┘
```

| 路径 | 职责 |
|---|---|
| `electron/` | 主进程、preload、路径与 Java 桥；新增 `dialog:pick-files` / `file:read-file` / `shell:open-external` / `window:set-full-screen` IPC |
| `frontend/` | UI、状态、导出、学习/日历/复习交互；TipTap 新增 `FileBlock`（附件预览）、`VideoBlock`（视频嵌入三视图）节点 |
| `backend/` | REST、导入解析、SRS、AI 代理、SQLite；新增 `note_attachment` 表与附件上传/列表/内容/删除 API |
| `resources/` | 示例题库等静态资源 |
| `scripts/` | 启动、smoke、便携审计、辅助脚本 |
| `docs/` | 专题说明（导入/导出/打包/附件等） |

更细的模块说明见 [docs/architecture.md](docs/architecture.md)。

---

## 4. 本地开发与验证

### 4.1 环境

- Node.js 20+  
- JDK 17+（Maven 3.9+ 或仓库内 `mvnw.cmd`）  
- Rust 工具链（**仅在开发/测试本地 embedding worker 时需要**；普通构建与 BM25/远程 embedding 流程不强制）  

### 4.2 常用命令

```powershell
npm install
npm run build:backend
npm run dev                 # Vite + Electron
npm run build:frontend
npm run build:electron
npm test
npm run test:rag              # 串联 embedding-worker(Rust) + backend + frontend 测试
pwsh -File scripts/smoke-mvp.ps1
pwsh -File scripts/portable-audit.ps1
pwsh -File scripts/mvp-test.ps1   # 后端核心接口冒烟（v0.5+）
python scripts/seed-test-bank.py  # 造题种子（v0.5+）
```

### 4.3 便携打包

1. 按 [docs/jlink.md](docs/jlink.md) 生成 `jre\bin\java.exe`。  
2. `npm run package:portable` → 输出在 `dist\`。  
3. 无 JRE 时脚本应拒绝打包。

### 4.4 建议自测清单

1. 导入 `resources/sample-bank.md` → 刷题 → 故意答错 → 错题出现。  
2. 笔记本插入公式/Mermaid → 默认渲染、点击可编辑。  
3. 深色模式：设置页描述列表、笔记本正文、AI 侧栏标题/输入/会话列表文字可读。  
4. 日历加入计划 → 今日队列可见 → 去学习。  
5. 背题评分后「仅待复习」列表变化。  
6. AI 多会话：新建、发消息、重启后消息仍在。  
7. **背知识点**：导入知识点 → 点 **AI 总结** → 「总结当前知识库」 → 卡片正文被浓缩、列表顶部出现「显示原文 / 显示总结」切换 → 切换有效。  
8. **单卡全屏视图**：点卡片本体 → 全屏覆盖视图 → 「重新总结」从原文重跑 → `Escape` 退出。  
9. **导出 round-trip**：题库导出 `.md` → 用「导入 Markdown」入口再导回 → 题目再现（无需改格式）。  
10. **冒烟脚本**：`pwsh -File scripts/mvp-test.ps1` 跑完后端核心接口回归。  
11. **循环背诵（v0.5.1）**：练习 → 背诵 → 背题，默认设置开始 → 跳转面板出现「第 1/3 轮…」分组 → 故意答错一题 → 该题在本组末尾橙色虚线重现 → 全部轮次完成后弹会话推荐。  
12. **背知识点选材（v0.5.1）**：背诵 → 背知识点 → 勾选一个章节父节点 → 「已选 N 个叶子卡片」只数叶子 → 开始后背诵条目不含章节标题卡。  
13. **题库选题继承（v0.5.1）**：题库页勾选 2 题 → 开始练习 → 刷题页显示「已选题目：2 题（可继续调整）」且仅这 2 题开局。  
14. **顽固项加练（v0.5.1）**：背诵会话里对同一题连点「不会」≥2 次 → 全部轮次完成后弹窗底部出现「顽固项加练（1）」且默认勾选 → 提交 → 日历明天与后天各出现一个「顽固项加练 · 第 N 天」计划组。  
15. **日历实时今天（v0.5.1）**：把系统时间调到 23:59 打开日历（高亮今天）→ 时间过零点后最迟 60 秒内今天高亮与下方队列切到新一天；先手动点选其它日期再跨零点，选中日保持不变。  
16. **界面状态记忆（v0.5.2）**：首次启动（或清除记忆后）应落在**笔记本**。随后逐个页面制造状态：题库页勾选题目 → 练习页切到「背诵 → 背知识点」并勾选范围 → 日历翻到上个月 → 错题页勾选几题，**最后停在知识点页**搜索并选中一张卡 → 完全退出应用再打开 → 应直接回到**知识点页**且搜索词/选中卡片已恢复；再依次切到练习、题库、错题、日历，确认背诵子模式与勾选范围、题库勾选、错题勾选、日历年月全部恢复。  
17. **深链与开关（v0.5.2）**：从日历「去学习」进入背题会话后直接退出应用 → 再次启动**不应**自动开始会话，而是回到「练习 → 背诵」的选材界面；设置里关闭「记住上次停留位置与各页选择」→ 已存记忆立即清空，重启回到笔记本且各页归零。  
18. **主题跟随系统（v0.5.3）**：设置 → 常规 → 主题选「跟随系统」→ 切换 Windows 深浅色 → 界面随之变化，重启后仍跟随；点顶栏明暗开关 → 单选自动回到对应显式模式。  
19. **自定义快捷键（v0.5.3）**：设置 → 常规 → 快捷键，「打开 / 关闭 AI 助手」点「+」→ 按 `Ctrl+K` → 出现 Ctrl+J、Ctrl+K 两组按键块，两键都能开合侧栏；点 Ctrl+J 的 × → 顶栏提示变为「AI · Ctrl+K」，`Ctrl+J` 不再打开侧栏；再给「切换深浅主题」录 `Ctrl+K` → 提示冲突并保持原绑定；录单个字母 → 提示需含 Ctrl 或 Alt；「折叠 / 展开大纲」录 `O` → 知识卡片全屏按 `O` 与 `T` 都能折叠大纲；「提交答案」清空 → 刷题页 `Enter` 不再提交、底部提示不再显示提交键；点「全部恢复默认」→ 全部回到出厂。关闭「显示 AI 悬浮球」后快捷键仍可用。  

---

## 5. 文档索引

| 文档 | 内容 |
|---|---|
| [docs/architecture.md](docs/architecture.md) | 架构、模块边界、数据与主题 |
| [docs/import-formats.md](docs/import-formats.md) | PDF / JSON 等导入格式 |
| [docs/knowledge-point-import.md](docs/knowledge-point-import.md) | 知识点 Markdown 导入 |
| [docs/quiz-markdown-flow.md](docs/quiz-markdown-flow.md) | 刷题 Markdown 解析与渲染 |
| [docs/review-srs.md](docs/review-srs.md) | 间隔重复复习（SM-2 风格）：数据库、API、今日队列、日历叠加 |
| [docs/session-curve.md](docs/session-curve.md) | 会话内短周期记忆曲线（循环背诵）：配置、队列算法、页面交互、兼容边界、会话↔日历/跨天曲线联动（评分最终态、顽固项加练、日历实时今天）、测试 |
| [docs/jlink.md](docs/jlink.md) | 便携包嵌入 JRE |
| [docs/ai-summary.md](docs/ai-summary.md) | AI 总结知识点：schema、REST API、前端交互 |
| [docs/ai-retrieval.md](docs/ai-retrieval.md) | 笔记本混合检索（RAG）：架构、schema v8、API、worker 协议、模型目录、隐私授权、降级、索引维护 |
| [docs/testing/QA_CHECKLIST.md](docs/testing/QA_CHECKLIST.md) | 功能验收与回归测试手册（分模块用例表） |
| [docs/testing/TESTING_GUIDELINES.md](docs/testing/TESTING_GUIDELINES.md) | 测试编写规范与提交前三道质量门禁 |
| [docs/testing/BUG_REPORT_TEMPLATE.md](docs/testing/BUG_REPORT_TEMPLATE.md) | 缺陷报告模板 |

---

## 6. 约束与安全

- **便携**：用户数据、日志、缓存应落在应用根目录策略下（开发态见启动脚本使用的便携数据目录）。  
- **网络**：业务 API 仅本机回环；外发仅为用户配置的 AI Endpoint。  
- **密钥与会话**：API Key 与 AI 消息加密存储；前端不长期持有明文 Key。  
- **检索隐私**：BM25 与本地 embedding 不出本机；远程 embedding（OpenAI/Ollama）仅在用户当前授权下发送笔记内容，授权绑定 endpoint/model（见 [docs/ai-retrieval.md](docs/ai-retrieval.md)）。  
- **健康检查**：生产构建避免在健康接口暴露敏感路径。  

---

## 许可证

见仓库根目录 `LICENSE`。

# 功能验收与回归测试手册 (QA Checklist)

本文档定义了 QA 测试人员、产品及开发人员在发版或功能合并前的验收用例与回归步骤。

---

## 模块一：知识点树状阅读与卡片工作区

| 用例编号 | 功能特性 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **TC-KP-01** | 大纲总根目录 | 当前题库已存在知识点 | 打开「知识点」页面，查看左侧大纲树最顶层 | 1. 最顶层显示题库名称的总根目录（带图标）<br>2. 所有一级章节收纳其下，支持折叠/展开 | P0 |
| **TC-KP-02** | 全文总览阅读 | 当前题库存在多个章节/知识点 | 在左侧大纲点击最顶层的「总根目录」 | 1. 右侧卡片工作区递归拼接当前题库下的全部章节与内容<br>2. 按照文档层级以 Markdown 标题与正文形式完整展现 | P0 |
| **TC-KP-03** | 树状大纲多级导航 | 题库已导入含 2~3 级标题的知识卡片 | 1. 点击某子节点<br>2. 观察面包屑与卡片展示 | 1. 右侧工作区即时切换至该知识点内容<br>2. 面包屑路径显示正确祖先链，Hover 可查看完整路径 | P0 |
| **TC-KP-04** | 大纲搜索过滤 | 题库存在多个知识点 | 在顶部搜索框输入关键词 | 1. 左侧大纲自动过滤并展开匹配节点<br>2. 未匹配的子树隐藏，匹配项正常高亮 | P1 |
| **TC-KP-05** | 全屏阅读态 | 处于知识点阅读界面 | 1. 点击卡片右上角「全屏」按钮<br>2. 观察全屏页面布局 | 1. 弹出全屏覆盖层<br>2. 左侧常驻大纲树，右侧展示卡片大字号正文 | P0 |
| **TC-KP-06** | 全屏大纲折叠与展开 | 处于全屏阅读态 | 1. 点击工具栏「折叠大纲」按钮或按下快捷键 `T`<br>2. 再次点击或按 `T` | 1. 左侧大纲收起，右侧正文区域居中拓宽<br>2. 再次操作大纲重新滑出展开 | P0 |
| **TC-KP-07** | 全屏键盘快捷翻页 | 处于全屏阅读态 | 按下键盘左箭头 `←` 或右箭头 `→` | 1. 自动切换到上一个或下一个知识点卡片<br>2. 左侧大纲高亮跟随变动 | P1 |
| **TC-KP-08** | 单卡删除联动 | 选中某个知识点卡片 | 点击卡片右上角「删除」并确认 | 1. 提示删除成功<br>2. 左侧大纲移除该节点<br>3. 右侧工作区重置为空白选择提示，不残留已删卡片 | P0 |
| **TC-KP-09** | 题库一键清空 | 题库中存在历史知识点数据 | 点击顶部操作栏「清空知识库」危险按钮，在气泡框中点击确认 | 1. 提示已清空 X 个知识点<br>2. 左侧大纲与右侧工作区全部重置为空状态<br>3. 数据库关联记录级联清理 | P0 |
| **TC-KP-10** | Markdown 增量导入 | 准备包含 `# 标题` 的 Markdown 文本文件 | 点击顶部「导入 Markdown」选择文件上传 | 1. 后端按标题逐级增量解析为知识卡片<br>2. 导入成功后自动重构完整的大纲树并渲染 | P0 |
| **TC-KP-11** | 零数据空状态 | 当前选择的题库无任何知识点 | 切换到一个空的题库 | 1. 页面不崩溃报错<br>2. 顶部「清空知识库」按钮自动隐藏<br>3. 提示选择/新建知识点或导入 | P1 |

---

## 模块二：SRS 记忆曲线与循环背诵

> 会话内短周期记忆曲线（多轮循环 + 错题重复）的设计说明见 [docs/session-curve.md](../session-curve.md)；跨天 SM-2 见 [docs/review-srs.md](../review-srs.md)。

| 用例编号 | 功能特性 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **TC-SRS-01** | 深链背诵评分 | 从日历/今日队列深链进入知识点或背题会话 | 1. 点击查看内容<br>2. 点击「会」或「不会」 | 1. 评分正常写入 SM-2 调度记录（**首次评分提交；同会话内仅判定翻转才再补交一次**，带 `forceAdvance`）<br>2. 深链/定时任务会话强制**单轮**：每条只出场一次，不循环多遍 | P0 |
| **TC-SRS-02** | 会话队列完成推荐 | 完成一轮背诵队列 | 答完最后一题/最后一张卡 | 弹出背诵完成提示及跨天复习计划推荐弹窗 | P1 |
| **TC-SRS-03** | 练习页两级结构 | 侧栏打开「练习」 | 1. 查看 Tab 结构<br>2. 背诵 Tab 内切换子模式 | 1. Tab 为「刷题 / 背诵」两级<br>2. 背诵内含「背题（题库）/ 背知识点」子切换，切换后互不污染状态 | P0 |
| **TC-SRS-04** | 记忆曲线设置弹窗 | 处于背题或背知识点选材页 | 1. 点「记忆曲线 · 摘要」按钮<br>2. 应用预设「强化 · 五轮循环」→ 保存 | 1. 弹窗含开关/轮数/策略/上限/连对/跳过/次轮顺序全部选项<br>2. 保存后按钮摘要更新为「循环 5 轮…」，刷新后仍保持（localStorage 持久化） | P0 |
| **TC-SRS-05** | 多轮循环出场 | 循环轮数=3，已选 N 个条目 | 开始背诵后观察跳转面板与进度 | 1. 面板按「第 1/3 轮…第 3/3 轮」分组，每轮各 N 条<br>2. 进度条显示蓝色「第 r / 3 轮」标签<br>3. 所选条目每轮均按既定顺序出场一次 | P0 |
| **TC-SRS-06** | 组末复习（默认策略） | 每组题数=10 | 在某组第 3 题点「不会」 | 该题插入**本组末尾**再次出场（面板中橙色虚线重现位），组间其他题不受影响 | P0 |
| **TC-SRS-07** | 策略切换：本轮末尾/延迟 | 已切换错题策略 | 分别用「本轮末尾」「延迟重现(间隔3)」各答错一题 | 1. 本轮末尾：插到本轮最后一个条目之后<br>2. 延迟重现：隔 3 个条目后出现，且重现位置不越过本轮边界 | P1 |
| **TC-SRS-08** | 额外重复上限 | 上限=1 | 同一条目连续答错 2 次 | 第 1 次错追加 1 个重现；第 2 次错显示「本次未记住」，不再额外重复，但**后续轮次仍正常出场** | P1 |
| **TC-SRS-09** | 不限重复 | 上限=0（直到会为止） | 同一题连续答错多次 | 持续按策略重现直至答对，无「放弃」标记 | P2 |
| **TC-SRS-10** | 次轮错题优先 | 轮数≥2，次轮顺序=错题优先 | 第 1 轮把靠后的 B 题答错并答完本轮 | 第 2 轮跳转面板中 B 排在该轮第一位，其余保持原序 | P1 |
| **TC-SRS-11** | 过关跳过后续轮 | 开启「过关后跳过后续轮次」 | 某条目答对过关 | 该条目从后续轮次移除（跳转面板轮内计数减少），全部过关后会话提前结束 | P2 |
| **TC-SRS-12** | 背知识点仅叶子 | 知识树含章节父节点与叶子卡片 | 勾选某中间节点开始背诵 | 1. 计数与「已选 N 个叶子卡片」只统计叶子<br>2. 背诵条目不含父节点/章节节点（标题不重复出现纯容器卡） | P0 |
| **TC-SRS-13** | 刷题模式不受新配置影响 | 循环轮数=3、策略=组末复习 | 进入刷题答错一题 | 仍为**单轮 + 隔 3 题延迟重现**（旧行为），无轮次标签 | P0 |
| **TC-SRS-14** | 题库开始练习继承选题 | 题库页勾选部分题目 | 点「开始练习」 | 1. 跳转刷题页显示绿色「已选题目：N 题（可继续调整）」<br>2. 选题器预选这些题，可增减后开始<br>3. 未勾选任何题时按整库进入；错题页「错题再练」仍显示「错题再练：N 题」 | P0 |
| **TC-SRS-15** | 跨天评分最终态 | 默认曲线配置，选 1 个条目开始背诵（3 轮） | 1. 第 1 轮点「会」<br>2. 第 2、3 轮再点「会」<br>3. 另开一次会话：第 1 轮点「不会」，重现条目上改点「会」 | 1. 重复同判定只提交一次 SM-2，`next_review` 不被反复推远<br>2. 判定翻转时补交一次且请求带 `forceAdvance: true`，当天该卡离开到期队列 | P0 |
| **TC-SRS-16** | 顽固项识别与加练卡片 | 额外重复上限 ≥2，会话内对同一条目连点「不会」≥2 次 | 答完全部轮次触发会话结束弹窗 | 1. 弹窗底部出现「顽固项加练（N）」且**默认勾选**，带「本会话重复答错」标签与条目名预览<br>2. 无顽固项时该卡片完全不渲染<br>3. 取消勾选后，若没有常规候选则提交按钮禁用 | P0 |
| **TC-SRS-17** | 顽固项写入日历 | 承 TC-SRS-16，保持勾选且「加入学习日历」开启 | 点提交后打开日历 | 1. 明天与后天各出现一个「顽固项加练 · 第 1 天 / 第 2 天」计划组，条目一致<br>2. 顽固项即使不在常规候选中也被「加入记忆曲线」（`session-apply` 的 enroll 全集含它，且不重复）<br>3. 今日队列对应日期可见 | P0 |
| **TC-SRS-18** | 日历实时今天 | 应用常驻日历页，跨过系统零点 | 1. 停留在「今天」等跨零点<br>2. 另一次先手动点选其它日期再跨零点 | 1. 零点后 ≤60 秒今天高亮与到期/逾期标记切到新一天，计划、到期统计、今日队列重新拉取<br>2. 选中日在今天时自动跟随到新一天（URL 不带 `?date=`）；手动选中其它日期时选中日不变 | P1 |

---

## 模块三：数据管理（设置 → 数据管理）

| 用例编号 | 功能特性 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **TC-DM-01** | 数据管理入口 | 应用正常运行 | 打开「设置」→「数据管理」Tab | 备份管理状态行、本地备份卡、数据导出/导入区域完整渲染，无控制台报错 | P0 |
| **TC-DM-02** | 手动备份 | 数据管理 Tab 已打开 | 点击「立即备份」 | 1. 列表新增 zip 条目（文件名含时间戳）<br>2. 状态行「最近备份」时间更新<br>3. 备份目录生成 zip，内含 manifest.json + study.db（非精简另含 attachments/） | P0 |
| **TC-DM-03** | 精简备份 | 存在笔记本附件 | 开启「精简备份」后再次备份 | 1. zip 内不含 attachments/<br>2. 列表条目带「精简」标识 | P1 |
| **TC-DM-04** | 自动备份配置与到期 | 数据管理 Tab 已打开 | 自动备份选「12 小时」并保存 | 1. 配置保存成功，状态行显示下次备份时间<br>2. 到期后自动生成备份并计入保留策略 | P1 |
| **TC-DM-05** | 保留策略清理 | 最大备份数设为 3 | 连续创建 4 个及以上备份 | 最早的备份被自动删除，目录内备份数不超过上限 | P1 |
| **TC-DM-06** | 备份目录自定义 | Electron 客户端运行 | 点击「浏览」选择自定义目录 → 备份 | 1. 弹出目录选择器<br>2. 后续备份落入选定目录<br>3.「恢复默认」可回到默认备份目录 | P1 |
| **TC-DM-07** | 数据导出 | 存在有效备份或数据 | 点击「数据导出」选择保存路径 | 指定路径生成完整 zip；该文件不参与保留策略自动清理 | P1 |
| **TC-DM-08** | 数据导入 | 准备合法备份 zip | 点击「数据导入」选择 zip → 确认覆盖警告 | 1. 提示导入暂存成功、重启后生效<br>2. 重启应用后数据与附件为备份内容 | P0 |
| **TC-DM-09** | 备份列表内恢复 | 备份列表存在条目 | 点击「恢复」并在警告弹窗确认 | 同导入：暂存 + 重启换库生效；若恢复失败，旧库保留可正常启动 | P0 |
| **TC-DM-10** | 删除备份 | 备份列表存在条目 | 点击「删除」并确认 | 条目消失，磁盘上对应 zip 文件一并删除 | P1 |
| **TC-DM-11** | 打开备份目录 | Electron 客户端运行 | 点击「打开目录」 | 系统资源管理器打开当前备份目录 | P2 |
| **TC-DM-12** | 非法输入边界 | — | 1. 导入非 zip / 缺 study.db 的文件<br>2. 备份目录指向无法创建的路径 | 1. 明确错误提示（400/422 文案）<br>2. 不产生半成品文件，原数据不受影响 | P1 |
| **TC-DM-13** | 备份并发防重 | 一次备份正在进行 | 备份过程中再次点击「立即备份」 | 第二次请求不会并发执行（提示进行中/稍后再试），最终不产生损坏文件 | P2 |

---

## 模块四：AI 连接配置（设置 → AI 连接）

| 用例编号 | 功能特性 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **TC-AIC-01** | 配置卡结构 | 设置 → AI 连接 | 查看主模型卡与导入兜底模型卡 | 两卡均含：API 格式下拉、Base URL、模型、API Key、流式开关、获取模型、保存；不再显示 Provider 字段 | P0 |
| **TC-AIC-02** | API 格式切换 | AI 连接卡已打开 | 在下拉中切换 OpenAI (Chat Completions) / Anthropic (Messages) | 1. Base URL 占位提示随格式变化（Anthropic 提示可省略 /v1）<br>2. 保存后重进设置回显所选格式 | P0 |
| **TC-AIC-03** | Anthropic 端点连通 | 拥有 Anthropic 格式可用的 Base URL / Key / 模型 | 保存后发起 AI 对话 | 对话正常返回（后端按 /v1/messages 协议请求成功） | P0 |
| **TC-AIC-04** | 获取模型 | 连接信息已填写（真实端点） | 点击「获取模型」 | 1. 弹出二级悬浮窗列出可用模型<br>2. 支持关键词模糊搜索<br>3. 点击模型名即选中回填 | P0 |
| **TC-AIC-05** | 模型逐项测活 | 已获取模型列表 | 对某模型点「测活」，可先修改测活提示词 | 显示延迟(ms)与回复摘要；失败时显示具体错误原因 | P0 |
| **TC-AIC-06** | 流式开关 | AI 连接已保存 | 关闭「流式传输」保存后发起对话 | 回复一次性返回，无逐字输出与思考链流式效果 | P0 |
| **TC-AIC-07** | Key 保密回显 | 已保存过 API Key | 保存后重进设置 | Key 显示「已配置，留空表示不修改」，不回显明文；留空保存不覆盖旧 Key | P0 |
| **TC-AIC-08** | 导入兜底独立 | 仅填写导入兜底卡 | 执行一次 PDF/Markdown 导入 | 导入兜底模型生效，主对话仍走主模型卡配置 | P1 |
| **TC-AIC-09** | mock 端点 | — | Base URL 填 mock://local | 对话/总结可用（本地模拟回复）；「获取模型」按钮置灰禁用 | P1 |

---

## 模块五：AI 对话流式与中断（AI 侧栏）

| 用例编号 | 功能特性 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **TC-AIT-01** | 流式输出 | 已配置可用模型（流式开启） | `Ctrl+J` 打开侧栏发送消息 | 回复逐字流式出现在消息区 | P0 |
| **TC-AIT-02** | 思考链流式 | 使用支持 CoT 的模型 | 发送消息并观察 | 思考链逐段显示在可折叠「思考链」区域，正文随后流式输出 | P0 |
| **TC-AIT-03** | 思考链持久化 | 存在含思考链的历史对话 | 关闭侧栏重新打开（或重启应用） | 历史消息仍可展开查看当时的思考链内容 | P0 |
| **TC-AIT-04** | 中断·思考链阶段 | 生成中且处于思考链输出 | 点击发送键位置的中断按钮 | 1. 流停止，按钮恢复发送态<br>2. 保留用户消息与已生成思考链并落库 | P0 |
| **TC-AIT-05** | 中断·正文阶段 | 正文正在流式输出 | 点击中断按钮 | 保留已生成正文（与思考链）一并落库，无残留占位符 | P0 |
| **TC-AIT-06** | 中断·零输出 | 刚发送、尚无任何输出 | 立即点击中断 | 占位消息与用户消息回滚，会话无残留 | P1 |
| **TC-AIT-07** | 中断后可继续 | 刚完成一次中断 | 再次输入并发送 | 正常开始新一轮流式回复，无卡死/无输入冻结 | P0 |
| **TC-AIT-08** | 新建会话直跳 | AI 侧栏已打开 | 点击「新建会话」 | 立即切换到空白新会话，无「已新建会话」提示，焦点可直接输入 | P0 |
| **TC-AIT-09** | 空白会话复用 | 当前已是空白新会话 | 再次点击「新建会话」 | 不重复创建会话，停留在当前空白会话 | P0 |
| **TC-AIT-10** | 删除会话输入焦点 | Electron 客户端运行 | 删除某会话并在确认弹窗点击确定 | 删除成功且输入框可正常键入（不出现键盘失灵、无需切换窗口恢复） | P0 |
| **TC-AIT-11** | 侧栏伸缩 | AI 侧栏已打开 | 拖拽侧栏左缘 | 宽度实时变化且有边界限制；重开侧栏后保持上次宽度 | P1 |
| **TC-AIT-12** | 切换会话流隔离 | 会话 A 正在流式输出 | 流式中切换到会话 B 并发消息 | A 的流不写入 B；B 正常对话互不污染 | P1 |

---

## 模块六：设置页 Tab 改版

| 用例编号 | 功能特性 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **TC-SET-01** | Tab 渲染 | 打开设置页 | 观察顶部 | 横向 Tab：常规 / AI 连接 / 嵌入与检索 / 学习与复习 / 数据管理；默认停在「常规」 | P0 |
| **TC-SET-02** | 切换与记忆 | 已切到任意 Tab | 切换后关闭设置页再进入 | 停留在上次的 Tab（URL 参数记录） | P1 |
| **TC-SET-03** | 键盘可达 | 设置页已打开 | Tab 栏聚焦后按 `←` / `→` | 焦点在 Tab 间移动并切换对应面板 | P2 |
| **TC-SET-04** | 深色主题适配 | 深色模式开启 | 逐个 Tab 查看 | 选中态反色清晰，面板内容对比度正常 | P1 |
| **TC-SET-05** | 便携面板移除 | — | 全设置页查找 | 不再出现「便携运行」面板及健康状态等路径信息展示（按需求移除） | P2 |
| **TC-SET-06** | 主题三选一 | 常规 Tab | 1. 「主题」选「跟随系统」→ 切换 Windows 深浅色<br>2. 点顶栏明暗开关 | 1. 界面随系统实时切换，重启后仍为跟随系统<br>2. 单选自动回到对应的显式模式（浅色 / 深色） | P1 |
| **TC-SET-07** | AI 悬浮球开关 | 常规 Tab | 关闭「显示 AI 悬浮球」 | 右下角悬浮球消失；`Ctrl+J`（或当前绑定）与常规里「打开 AI 助手」按钮仍可打开侧栏；重启后保持隐藏 | P1 |
| **TC-SET-08** | 快捷键改绑（多绑定） | 常规 Tab → 快捷键 | 1. 「打开 / 关闭 AI 助手」点「+」→ 按 `Ctrl+K`<br>2. 到任意页面按 `Ctrl+J`、`Ctrl+K`<br>3. 点 `Ctrl+J` 按键块旁的 × | 1. 行内出现 `Ctrl` `J` 与 `Ctrl` `K` 两组按键块，行内出现单项「恢复默认」图标，重启后保持<br>2. 两个组合键都能打开 / 关闭侧栏；顶栏提示与悬浮球 title 显示首个绑定<br>3. 仅剩 `Ctrl+K`，`Ctrl+J` 无反应，顶栏提示变为 Ctrl+K | P0 |
| **TC-SET-09** | 快捷键规则与冲突 | 已完成 TC-SET-08 | 1. 给「切换深浅主题」录 `Ctrl+K`<br>2. 「打开设置」录单个字母 `A` 或 `Shift+A`<br>3. 「搜索大纲与正文」录 `Ctrl+K`<br>4. 「下一题」录 `Ctrl+S`（「提交答案」已有）<br>5. 「发送消息」录 `Ctrl+K`<br>6. 录制中按 `Esc`；再录制按 `Backspace`<br>7. 点「全部恢复默认」 | 1. 提示「已用于「打开 / 关闭 AI 助手」」，绑定不变<br>2. 提示需含 Ctrl 或 Alt，退出录制<br>3. 全局键不能再给页面动作，同样拒绝<br>4. 允许：同一页面不同阶段可共用<br>5. 提示只能使用 Enter<br>6. Esc 保持原值；Backspace 清空该项为「未设置」<br>7. 全部回到出厂，按钮变灰、单项图标消失 | P0 |
| **TC-SET-10** | 录制期间让路 | 快捷键面板 | 「切换深浅主题」点「+」进入录制 → 按 `Ctrl+J` | 侧栏**不**弹出，`Ctrl+J` 被当作待录制组合键并因冲突被拒绝 | P1 |
| **TC-SET-11** | 页面内快捷键改绑 | 常规 Tab → 快捷键 | 1. 「折叠 / 展开大纲」录 `O`，「提交答案」清空，「上一题」移除 `P`<br>2. 打开知识点全屏卡片按 `O`、`T`；悬停折叠按钮<br>3. 开始刷题按 `Enter`、`Ctrl+S`；看底部提示<br>4. 已提交后按 `Enter`、`P`、`←` | 1. 三行分别显示 `T` `O`、「未设置」、`←` `PgUp`<br>2. `O` 与 `T` 都能折叠 / 展开（多绑定并存），工具提示为「折叠大纲 (T / O)」<br>3. 都不提交；提示为「数字 1-4 选择，←/PgUp 上一题，Enter/→/PgDn/N 下一题 · Ctrl+J AI」<br>4. `Enter` 下一题；`P` 无反应，`←` 上一题 | P0 |
| **TC-SET-12** | 编辑器 / 题库 / AI 发送键改绑 | 常规 Tab → 快捷键 | 1. 「完成块编辑」改为 `Alt+Enter`；到笔记本编辑公式块按 `Ctrl+Enter`、`Alt+Enter`<br>2. 「重命名题库」改为 `Alt+R`；题库列表选中一项按 `F2`、`Alt+R`<br>3. 「发送消息」追加 `Ctrl+Enter` 后移除 `Enter`；AI 输入框按 `Enter`、`Ctrl+Enter` | 1. `Ctrl+Enter` 不再完成、`Alt+Enter` 完成；编辑器提示文案显示 Alt+Enter<br>2. `F2` 无反应，`Alt+R` 进入重命名<br>3. `Enter` 换行、`Ctrl+Enter` 发送；占位文案「Ctrl+Enter 发送 · Enter 换行」 | P1 |

---

## 模块七：笔记本编辑与保存

| 用例编号 | 功能特性 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **TC-NB-01** | 页面行内删除 | 笔记本存在多个页面 | 页面列表某行点删除图标 → 确认 | 该页删除并刷新列表与编辑区，相邻页面不受影响，无需先选中该页 | P0 |
| **TC-NB-02** | 无感自动保存 | 编辑器已有修改 | 修改正文后停顿约 0.5 秒 | 自动保存完成（约 400ms 防抖），界面无「已保存」文字打扰 | P0 |
| **TC-NB-03** | 退出兜底保存 | 刚输入 1 秒内 | 立即关闭应用窗口后重启 | 最近编辑大概率已落盘（尽力而为策略，不承诺 100%） | P1 |
| **TC-NB-04** | 新建笔记本 | 笔记本页面已打开 | 点击「新建笔记本」→ 输入标题 → Enter/确认 | 1. 弹出 Modal 创建成功并选中<br>2. 空标题提交给出警告，不创建 | P0 |
| **TC-NB-05** | 首次启动种子数据 | 笔记本数据为空 | 启动应用进入笔记本页 | 自动出现「默认笔记本 / 开始这里」，无需手动新建 | P1 |
| **TC-NB-06** | 编辑区页面标题改名 | 已选中某个页面 | 1. 在编辑区顶部标题框改名<br>2. 按 Enter（或点空白处失焦）<br>3. 另试改名后按 `Esc` | 1. 逐字输入不发请求、不被旧标题顶回<br>2. 提交后左侧列表与标题框同步为新名，只发一次 `PUT /api/note-pages/{id}`<br>3. `Esc` 回到原标题且不发请求；标题清空提交时提示「页面标题不能为空」并留在输入框 | P0 |
| **TC-NB-07** | 页面列表改名（双击 / F2） | 笔记本存在多个页面 | 1. 双击某行页面标题<br>2. 改名后点别处失焦<br>3. 另选一行按 `F2` | 1. 该行标题变输入框并全选，勾选框与删除按钮保持可用<br>2. 失焦提交并刷新列表；改名期间点该行不切换选中页<br>3. `F2` 同样进入改名（可在设置 → 常规 → 快捷键「重命名笔记页」改绑） | P0 |
| **TC-NB-08** | 笔记本改名 | 笔记本页已打开 | 1. 点下拉框右侧的改名图标按钮<br>2. 输入新名按 Enter<br>3. 再次进入后按 `Esc`；另试切换笔记本 | 1. 下拉框临时换成输入框并全选原名<br>2. 提交后下拉框与其它页面的笔记本选择器都显示新名<br>3. `Esc` / 切换笔记本都会收起输入框并恢复下拉，不发请求 | P0 |

---

## 模块八：界面状态记忆（v0.5.2）

> 设计与约束见 [docs/architecture.md](../architecture.md) §2.3 与 README §2.13。存储位置：`localStorage['ui.viewState.v1']`。

| 用例编号 | 功能特性 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **TC-VS-01** | 冷启动落笔记本 | 全新安装或已清除记忆 | 打开应用 | 落在「笔记本」页（不再是题库页），URL 为 `#/notebooks` | P0 |
| **TC-VS-02** | 重启回到上次页面 | 依次访问 知识点 → 练习 → 日历，最后停在日历 | 完全退出应用后重新打开 | 直接回到「日历」，不出现先闪题库页再跳转 | P0 |
| **TC-VS-03** | 各页选择与切换恢复 | 知识点页搜索并选中一张卡、练习页切到「背诵 → 背知识点」并**换过知识点库**且勾了部分卡片、题库页勾了 2 题、笔记本开专注模式 | 重启应用后逐个页面查看 | 搜索词/选中卡片/Tab 与子模式/背知识点所选知识库/背诵勾选范围/题库勾选/专注模式全部恢复；日历仍显示上次翻到的年月，选中日仍是今天 | P0 |
| **TC-VS-04** | 每库各自记忆 | 题库 A 勾 2 题、题库 B 勾 1 题 | 在 A/B 间来回切换；重启后再看 | 切回某库恢复它自己的勾选（不再被清空）；两库互不污染 | P1 |
| **TC-VS-05** | 深链不污染记忆 | 从日历「去学习」进入背题会话（URL 带 `questionIds`/`dayQueue=1`） | 会话中直接退出应用并重启 | 启动落在「练习」的选材界面：不自动开始会话、不带上次的题目清单；日历/今日队列深链的排程未被写进题库勾选记忆 | P0 |
| **TC-VS-06** | 失效记忆自愈 | 记住某题库/笔记本/页面/卡片后将其删除 | 重启应用并进入对应页面 | 回落该页默认第一项，不出现空白页、报错或「没有匹配题目」；已删除的筛选标签被剪掉 | P1 |
| **TC-VS-07** | 开关与清除 | 已有记忆 | 设置 → 通用 关闭「记住上次停留位置与各页选择」→ 重启；再打开开关 → 重启 | 关闭时立即清空（重启落笔记本、各页归零），关闭期间不再写入；点「清除已记住的位置」后同样立即归零且开关保持开启 | P1 |
| **TC-VS-08** | 敏感与会话态不外泄 | 刷题页输入过题库主密码、笔记页有未保存内容、背题做到一半 | 检查 `localStorage['ui.viewState.v1']` 内容并重启 | 存储中不含密码、不含笔记正文、不含队列/已答状态；重启后笔记仍走原有自动保存，会话需重新开始 | P0 |

---

## 模块九：自动化覆盖对照（v0.5.x–v0.6.1 新增功能）

| 被测功能 | 自动化位置 | 覆盖内容 |
| :--- | :--- | :--- |
| 数据备份/恢复 | `backend/src/test/java/com/drillnotebook/app/DataBackupServiceTest.java`（7 用例） | 精简/完整打包内容、保留策略清理、配置校验与往返、恢复暂存+重启换库、导入暂存+非法 zip 拒绝（半成品清理）、状态基线 |
| AI 上游双格式 | `backend/src/test/java/com/drillnotebook/app/service/AiUpstreamClientTest.java`（7 用例） | 端点 URL 归一、Chat Completions / Anthropic 双格式 payload 转换、SSE 流式解析（含思考链增量）、错误分类文案 |
| 会话内记忆曲线 | `frontend/src/lib/sessionCurve.test.ts`（20 用例） | 多轮队列构建、组末/本轮末尾/延迟三种插入策略与轮边界、重复上限与放弃、连对过关、skipPassed 剪枝、次轮错题优先、逐轮独立评分、配置钳位与摘要文案、顽固项阈值与真实会话推导 |
| 背诵设置弹窗 | `frontend/src/components/SessionCurveSettingsModal.test.tsx`（3 用例） | 选项渲染、预设应用与保存写入 localStorage、每次打开重读最新配置 |
| 会话结束顽固项加练 | `frontend/src/components/SessionPlanRecommendModal.stubborn.test.tsx`（4 用例） | 加练卡片默认勾选、提交明天+后天两组并去重并入 enroll 全集、取消勾选后禁止提交、无顽固项不渲染 |
| 背诵评分最终态 | `frontend/src/pages/knowledge/KnowledgeMemorizeSession.test.tsx`（3 用例） | 首次评分提交、判定翻转才补交（`forceAdvance`）、多轮重复同判定不再提交 |
| 错题本频率与移出 | `backend/src/test/java/com/drillnotebook/app/repository/QuestionWrongBookTest.java`（6 用例，真库） | 答错计入 `wrongCount`、答错后答对 1 次仍在（修「重复到对」陷阱）、连续答对 2 次自动清出、手动移出后重答错重新计入（`wrong_excluded` 重置）、按错误次数降序、错误率计算 |
| 日历实时今天 | `frontend/src/lib/useToday.test.tsx`（2 用例）+ `frontend/src/pages/CalendarPage.midnight.test.tsx`（4 用例） | 轮询与窗口聚焦校准日期；打开时高亮今天、跨零点选中日跟随并重新拉数、手动选其他日期不被跳转、记忆里翻到的上月视图不被零点拽回 |
| 界面状态存储层 | `frontend/src/lib/viewState.test.ts`（14 用例） | 损坏 JSON/未知页/非法 id 容错、数组截断、防抖合并写入与去重、开关关闭不读不写、`recordRoute` 白名单与练习别名折叠、全选哨兵与 `all`/`some:[]` 往返、作用域按 `recent` 淘汰最久未写的库且永不淘汰正在写入的库 |
| 启动落点 | `frontend/src/App.startRoute.test.tsx`（6 用例） | 无记忆落笔记本、重启回上次页、未知路径回落、非法记忆忽略、损坏缓存、深链优先 |
| 题库页选择记忆 | `frontend/src/pages/BankPage.viewState.test.tsx`（4 用例） | 恢复上次题库与部分勾选、`all` 哨兵展开整库、题库已删回落第一个库、无记忆时不勾选 |
| 题库→刷题选题继承 | `frontend/src/pages/BankPage.workspace.test.tsx`（BNK-16 / BNK-16b） | 无勾选仅携带 bankId；有勾选携带 `questionIds`+`from=bank` |
| 笔记本创建 | `frontend/src/pages/NotebookPage.workspace.test.tsx`（NBK-14） | 「新建笔记本」弹窗 POST `/api/notebooks` 并切换到新笔记本；空标题不发请求 |
| 笔记本 / 页面重命名 | `frontend/src/pages/NotebookPage.workspace.test.tsx`（NBK-18，5 用例） | 编辑区标题本地成稿、回车只 PUT 一次且不被旧值顶回、`Esc` 放弃草稿；列表双击标题失焦提交、`F2` 进入改名且空标题被拒；笔记本改名按钮把下拉换成输入框并 PUT `/api/notebooks/{id}` |
| 全屏卡片快捷键不滚动 | `frontend/src/pages/knowledge/KnowledgeFullCardView.test.tsx`（3 用例） | ←/→/T/Esc 命中即 `preventDefault`（`fireEvent` 返回 false）、未绑定键不拦截、改绑到 ↑↓/PageUp/PageDown 同样不滚动、输入框内方向键与字母键交还控件 |
| RAG 索引状态自愈 | `backend/.../EmbeddingJobExecutorTest.java`（空语料激活 / supersede 后激活 / 有待跑任务不激活 / 无 provider 也能结算空语料 4 用例） | REBUILDING + 队列无可运行任务时重算 coverage 并激活；退避窗口内的任务与有排队任务时不会误激活 |
| normalizer 升级迁移 | `backend/.../NoteIndexingServiceTest.java`（2 用例） | 启动审计重建 hash 不符的页（视频页从 0 分块变为可检索）、二次运行幂等；hash 一致的页与笔记正文不被改写 |
| 视频 / 附件块入索引 | `backend/.../NoteNormalizerTest.java`（3 用例） | `videoBlock` 取标题 + URL、`fileBlock` 取文件名、空 attrs 不产生分块 |
| provider 可用性可见 | `backend/.../EmbeddingJobExecutorTest.java` + `frontend/src/components/EmbeddingSettingsCard.test.tsx` | status 返回 `providerReady`；`REBUILDING && providerReady=false` 时设置页显示「本地向量组件不可用」而非「索引构建中」 |
| 前端回归 | `npm test --prefix frontend`（Vitest 全量 316 项） | AI 流式解析、设置面板、记忆曲线、会话↔日历联动、界面状态记忆、题库/知识点/背诵组件交互回归、Markdown 题干纯文本化（`markdownText`）、`app.css` 令牌约束（Embedding 条目跟主题变量） |
| 全量门禁 | `.\mvnw.cmd test`（399 项）+ `npm run build --prefix frontend` | 提交前三道门禁，见 TESTING_GUIDELINES.md 第二节 |

---

## 模块十：v0.6 UI 视觉回归

> 设计令牌与几何契约见 [DESIGN.md](../../DESIGN.md)；图标产物由 `npm run build:icons` 生成到 `resources/icon/`。

| 用例编号 | 功能特性 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **TC-UI-01** | 品牌与图标 | 打包或 `npm run dev` | 看窗口 / 任务栏图标与侧栏品牌行 | 均为新图标（渐变方块 + 折角页 + 对勾）；浏览器标签页 favicon 同款 | P1 |
| **TC-UI-02** | 分组侧栏 | 任意页面 | 1. 观察侧栏<br>2. 点「收起侧栏」→ 悬停图标 → 等 3 秒 → 鼠标贴左缘 | 1. 资料（笔记本 / 题库 / 知识点）、学习（练习 / 错题）、规划（日历）三组，设置在底部，当前页浅蓝高亮<br>2. 56px 窄栏图标居中并显示名称提示；3 秒后自动隐藏，贴左缘唤回（行为与 v0.5 一致） | P0 |
| **TC-UI-03** | 顶栏 | 任意页面 | 1. 切换页面看标题<br>2. 点「AI 助手」药丸<br>3. 点右侧圆形明暗按钮 | 1. 标题随页面变化<br>2. 药丸显示当前首个绑定（默认 Ctrl+J），点击打开侧栏<br>3. 明暗即时切换，设置 → 常规「主题」单选同步 | P0 |
| **TC-UI-04** | 明暗两态无残留 | 深色模式 | 逐页（笔记本 / 题库 / 错题 / 知识点 / 练习 / 日历 / 设置）并打开 Select 下拉、Modal、AI 侧栏 | 无纯白 / 纯黑残留，输入框、表格、弹层、Tag 均随令牌换色；文字对比度可读 | P0 |
| **TC-UI-05** | 练习题卡 | 有题库 | 刷题：选项 → 提交；背题：揭示答案 → 评分 | 顶部进度条随题号推进；选项字母徽章，选中 / 正确 / 错误三态清晰；反馈卡左侧强调条；评分按钮悬停显示语义色 | P1 |
| **TC-UI-06** | 日历 | 日历页 | 观察月份头部与今天格 | 今天日期为实心徽章、选中日浅蓝描边；月份标题与年 / 月选择器在 380px 侧栏内自动换行不溢出 | P1 |
| **TC-UI-07** | 工作台契约不变 | 笔记本 / 题库 | 1100 / 1440px 下观察 explorer 与命令行 | 232px explorer、命令行 min-height 44、路由留白 16/20/24 与 v0.5 一致；`BankPage.workspace` / `NotebookEditor.workspace` 测试通过 | P0 |

---

## 模块十一：错题本频率与会话结束返回（v0.6.1）

| 用例编号 | 功能特性 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **TC-WB-01** | 错题按错误次数排序 | 有题库 | 刷题对 A 题答错 2 次、B 题答错 1 次 → 打开错题 | 错题本按「错 N / 答 M」显示，A 排在 B 前；附错误率列；不再只看最近一次对错 | P0 |
| **TC-WB-02** | 连续答对自动清出 | 某题在错题本 | 对该题连续答对 2 次 | 第 2 次答对后该题从错题本消失（`WRONG_MASTER_STREAK=2`）；只答对 1 次仍在列 | P0 |
| **TC-WB-03** | 手动移出错题本 | 某题在错题本 | 点该行操作列 ✓「移出错题本」（或勾选后批量移出） | 该题立即不在列表；提示「已移出错题本，下次答错会重新计入」 | P0 |
| **TC-WB-04** | 移出后重答错重新计入 | 已手动移出某题 | 再答错该题一次 | 该题重新出现在错题本（`recordAnswer` 把 `wrong_excluded` 重置为 0） | P1 |
| **TC-WB-05** | 刷题内移出入口 | 刷题已提交某题 | 提交后点「移出错题本」 | 提示成功；错题页刷新后该题不在列 | P1 |
| **TC-WB-06** | 会话结束返回初始界面 | 刷题 / 背题 / 背知识点 | 做完最后一题 → 弹「本轮结束后的学习计划」→ 点「跳过」或提交计划后关闭 | 回到选题初始界面，不停在最后一题 | P0 |
